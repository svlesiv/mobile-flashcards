import React, { Component } from "react";
import { View, Text } from "react-native";

export default class NewDeck extends Component {
    render() {
      return (
        <View>
            <Text>This is a new deck</Text> 
        </View> 
      );
    }
}
